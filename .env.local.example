# Copy this file to .env.local and fill in the real value.
# Get the connection string from your Neon project dashboard:
# https://console.neon.tech → Project → Connection Details → "Direct connection"
# Use the URL that starts with `postgresql://` (Neon serverless accepts pooled too).
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
